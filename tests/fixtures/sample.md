# Sample document

This is a deliberately small synthetic Markdown file used in tests.

## Introduction

The shelf converts and splits documents like this one. Sections are bounded
by H2 headings.

This paragraph mentions VLAN and bridge configuration as searchable keywords.

## Configuration

Configuration text. Repeats the keyword VLAN once more so search counts match.

### Sub-section that should not split

Despite the H3, the splitter only operates on H2.

## Troubleshooting

Final section, mentions BGP as a unique searchable keyword.
